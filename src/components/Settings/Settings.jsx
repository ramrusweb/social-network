import SettingsStyle from './Settings.module.css';

const Settings = () => {
    return(
        <section className={SettingsStyle.settings_section}>
            Settings
        </section>
    );
}

export default Settings;